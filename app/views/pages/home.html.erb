<% if user_signed_in? %>
    <div class="jumbotron jumbotron-home">
        <div class="container">
            <h1>Welcome back!</h1>
        </div>
    </div>
    <div class="container">
      <div class="row">
        <% @spaces.each do |space| %>
          <%= render 'spaces/card', space: space %>
        <% end %>
      </div>
    </div>
<% else %>
    <div class="jumbotron jumbotron-home">
        <div class="container">
            <h1>Welcome to SnapYourSpace!</h1>
            <p>SnapYourSpace is the premiere workspace photo sharing app ever built in the history of mankind.</p>
            <%= link_to "Learn more »", "#", class: "btn btn-primary btn-lg" %>
        </div>
    </div>
    <div class="container">
        <div class="row">
          <% @spaces.each do |space| %>
            <%= render 'spaces/card', space: space %>
          <% end %>
        </div>
    </div>
<% end %>